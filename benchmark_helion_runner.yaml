bf16_layernorm:
  args: "--device=cuda --precision bf16 --kernel bf16_layernorm --output benchmark_bf16_layernorm.json"
  runner: helion

bf16_matmul:
  args: "--device=cuda --precision bf16 --kernel bf16_matmul --output benchmark_bf16_matmul.json"
  runner: helion
